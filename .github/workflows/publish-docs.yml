name: Publish Docs

on:
  push:
    branches: [main]

# Prevent concurrent publish runs. If a new push arrives while publish is in
# progress, cancel the older run so the site always reflects the latest commit.
concurrency:
  group: publish-docs-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write
  packages: read

jobs:
  build-docs:
    name: Build AsciiDoc documentation
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle.kts', '**/libs.versions.toml', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Build AsciiDoc docs
        run: ./gradlew asciidoctor --no-daemon

      - name: Upload AsciiDoc output
        uses: actions/upload-artifact@v4
        with:
          name: asciidoc
          path: build/docs/asciidoc/
          retention-days: 1

  build-detekt:
    name: Generate Detekt HTML reports
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Cache Gradle
        uses: actions/cache@v5
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ hashFiles('**/*.gradle.kts', '**/libs.versions.toml', '**/gradle-wrapper.properties') }}
          restore-keys: gradle-

      - name: Create test directories
        run: mkdir -p /tmp/kbeatz-test-library /tmp/kbeatz-test-data

      - name: Run check (builds Detekt HTML reports)
        run: ./gradlew buildBackends check --no-daemon --no-parallel
        env:
          CATALOG_LIBRARY_ROOT: /tmp/kbeatz-test-library
          DATA_DIR: /tmp/kbeatz-test-data

      - name: Collect Detekt HTML reports
        run: |
          mkdir -p detekt-reports
          for module in kbeatz-catalog kbeatz-cli kbeatz-common kbeatz-sources kbeatz-tagger; do
            report="$module/build/reports/detekt/main.html"
            if [ -f "$report" ]; then
              cp "$report" "detekt-reports/${module}.html"
            fi
          done

      - name: Upload Detekt reports
        uses: actions/upload-artifact@v4
        with:
          name: detekt-reports
          path: detekt-reports/
          retention-days: 1

  build-eslint:
    name: Generate ESLint HTML report
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 'lts/*'
          cache: 'npm'
          cache-dependency-path: kbeatz-ui/package-lock.json

      - name: Install dependencies
        working-directory: kbeatz-ui
        run: npm ci

      - name: Generate ESLint HTML report
        working-directory: kbeatz-ui
        # Continue on lint warnings/errors so the report is still published.
        # The CI job (ci.yml) enforces zero warnings; here we only collect the report.
        run: |
          npx eslint . --ext ts,tsx --report-unused-disable-directives \
            --format html --output-file ../eslint-report.html || true

      - name: Upload ESLint report
        uses: actions/upload-artifact@v4
        with:
          name: eslint-report
          path: eslint-report.html
          retention-days: 1

  scan-trivy:
    name: Trivy container scan (HTML report)
    runs-on: ubuntu-latest
    timeout-minutes: 5
    # This job depends on the Docker image being available on GHCR (story #303 / PR #322).
    # If the image is not yet published the scan step is skipped gracefully and the
    # publish job renders a placeholder instead.
    continue-on-error: true

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Extract catalog version
        id: version
        run: |
          VERSION=$(grep -m1 '^version\s*=' kbeatz-catalog/build.gradle.kts \
            | sed 's/.*"\(.*\)".*/\1/')
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Pull catalog image
        id: pull
        run: |
          IMAGE="ghcr.io/triplem/kbeatz-catalog:${{ steps.version.outputs.version }}"
          if docker pull "$IMAGE" 2>/dev/null; then
            echo "image=${IMAGE}" >> "$GITHUB_OUTPUT"
            echo "available=true" >> "$GITHUB_OUTPUT"
          else
            echo "available=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Run Trivy image scan (HTML)
        if: steps.pull.outputs.available == 'true'
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: ${{ steps.pull.outputs.image }}
          format: 'template'
          template: '@/contrib/html.tpl'
          output: 'trivy-report.html'
          severity: 'HIGH,CRITICAL'
          exit-code: '0'

      - name: Upload Trivy report
        if: steps.pull.outputs.available == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: trivy-report
          path: trivy-report.html
          retention-days: 1

  publish:
    name: Publish to GitHub Pages (gh-pages branch)
    runs-on: ubuntu-latest
    needs: [build-docs, build-detekt, build-eslint, scan-trivy]
    # Publish even if Trivy scan was skipped (image not yet on GHCR).
    # Fail if any of the three mandatory build jobs failed.
    if: always() && needs.build-docs.result == 'success' && needs.build-detekt.result == 'success' && needs.build-eslint.result == 'success'
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Download AsciiDoc output
        uses: actions/download-artifact@v4
        with:
          name: asciidoc
          path: site/docs/

      - name: Download Detekt reports
        uses: actions/download-artifact@v4
        with:
          name: detekt-reports
          path: site/detekt/

      - name: Download ESLint report
        uses: actions/download-artifact@v4
        with:
          name: eslint-report
          path: site/eslint/

      - name: Download Trivy report (optional)
        id: trivy-download
        uses: actions/download-artifact@v4
        with:
          name: trivy-report
          path: site/trivy/
        continue-on-error: true

      - name: Build site index
        run: |
          TRIVY_LINK=""
          TRIVY_STATUS=""
          if [ "${{ steps.trivy-download.outcome }}" = "success" ] && ls site/trivy/*.html 1>/dev/null 2>&1; then
            TRIVY_LINK='<li><a href="trivy/trivy-report.html">Trivy Container Scan Report</a></li>'
          else
            TRIVY_STATUS='<p class="placeholder">Trivy container scan report is not yet available. It will appear here once the Docker image is published to GHCR (depends on story #303).</p>'
          fi

          DETEKT_LINKS=""
          for module in kbeatz-catalog kbeatz-cli kbeatz-common kbeatz-sources kbeatz-tagger; do
            if [ -f "site/detekt/${module}.html" ]; then
              DETEKT_LINKS="${DETEKT_LINKS}<li><a href=\"detekt/${module}.html\">${module}</a></li>"
            fi
          done

          cat > site/index.html << 'HTMLEOF'
          <!DOCTYPE html>
          <html lang="en">
          <head>
            <meta charset="UTF-8">
            <meta name="viewport" content="width=device-width, initial-scale=1.0">
            <title>kbeatz - Project Documentation</title>
            <style>
              body { font-family: Arial, Helvetica, sans-serif; max-width: 900px; margin: 40px auto; padding: 0 20px; color: #333; }
              h1 { border-bottom: 2px solid #0066cc; padding-bottom: 8px; }
              h2 { margin-top: 32px; color: #0066cc; }
              ul { line-height: 1.8; }
              a { color: #0066cc; }
              .placeholder { background: #fff3cd; border: 1px solid #ffc107; padding: 12px 16px; border-radius: 4px; }
              footer { margin-top: 48px; font-size: 0.85em; color: #666; border-top: 1px solid #ddd; padding-top: 12px; }
            </style>
          </head>
          <body>
            <h1>kbeatz - Project Documentation</h1>
            <p>Published from the <code>main</code> branch.</p>

            <h2>Documentation</h2>
            <ul>
              <li><a href="docs/index.html">Project Docs (AsciiDoc)</a></li>
            </ul>

            <h2>Quality Reports</h2>

            <h3>Detekt - Static Analysis (Kotlin)</h3>
            <ul id="detekt-list">DETEKT_LINKS_PLACEHOLDER</ul>

            <h3>ESLint - Static Analysis (TypeScript / React)</h3>
            <ul>
              <li><a href="eslint/eslint-report.html">ESLint Report</a></li>
            </ul>

            <h3>Trivy - Container Security Scan</h3>
            <ul id="trivy-list">TRIVY_LINK_PLACEHOLDER</ul>
            TRIVY_STATUS_PLACEHOLDER

            <footer>
              Generated by GitHub Actions &bull; <a href="https://github.com/triplem/kbeatz">triplem/kbeatz</a>
            </footer>
          </body>
          </html>
          HTMLEOF

          # Substitute the dynamic sections into the template
          ESCAPED_DETEKT=$(printf '%s\n' "$DETEKT_LINKS" | sed 's/[&/\]/\\&/g; s/$/\\n/' | tr -d '\n')
          ESCAPED_TRIVY=$(printf '%s\n' "$TRIVY_LINK" | sed 's/[&/\]/\\&/g; s/$/\\n/' | tr -d '\n')
          ESCAPED_STATUS=$(printf '%s\n' "$TRIVY_STATUS" | sed 's/[&/\]/\\&/g; s/$/\\n/' | tr -d '\n')

          sed -i "s|DETEKT_LINKS_PLACEHOLDER|${ESCAPED_DETEKT}|g" site/index.html
          sed -i "s|TRIVY_LINK_PLACEHOLDER|${ESCAPED_TRIVY}|g" site/index.html
          sed -i "s|TRIVY_STATUS_PLACEHOLDER|${ESCAPED_STATUS}|g" site/index.html

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v4
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./site
          publish_branch: gh-pages
          force_orphan: false
          commit_message: "ci(scaffold): publish docs from ${{ github.sha }}"
